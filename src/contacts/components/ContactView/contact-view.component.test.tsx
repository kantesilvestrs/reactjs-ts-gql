import React from "react";

describe("Test: ContactView_component_render", () => {
  it("should do the following unit tests", () => {
    // REnders
    // renders correct data
    // renders not found with incorrect id
    // renders error when query error found
    // rerenders correct data when id is changed
  });
});
