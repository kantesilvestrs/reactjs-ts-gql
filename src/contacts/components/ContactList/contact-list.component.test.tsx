import React from "react";

describe("Test: ContactList_component_render", () => {
  it("should do the following unit tests", () => {
    // Renders
    // Renders grid component
    // on add renders add dialog
  });
});
