import React from "react";

describe("Test: ConstactsModule_component_render", () => {
  it("should do the following unit tests", () => {
    // Renders
    // should render default route
    // should render components
    // should switch between components
  });
});
