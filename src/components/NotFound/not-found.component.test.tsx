import React from "react";

describe("Test: NotFound_component_render", () => {
  it("should do the following unit tests", () => {
    // Test if renders
    // Test if correct text is shown
  });
});
